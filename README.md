# ArcadeShooterECS

Высокопроизводительный аркадный шутер с видом сверху, построенный на **Unity ECS (DOTS)**. Проект демонстрирует применение принципов Data-Oriented Design для создания эффективной и масштабируемой игровой логики.

---

## 🏗 Архитектура

Проект строго следует принципам **чистой ECS архитектуры**. Вся логика отделена от данных, а тяжелые вычисления оптимизированы с помощью Job System и Burst Compiler.

### Основные принципы:
- **Pure ECS**: Игровая логика реализована исключительно в `ISystem` и `SystemBase`. `MonoBehaviour` используется только для Authoring и связи с UI.
- **Job System & Burst**: Все ресурсозатратные системы параллелизированы через `IJobEntity`. Для всех систем и джобов включена Burst-компиляция.
- **Unity Physics**: Обработка коллизий и триггеров через `Unity.Physics`. Используется `ITriggerEventsJob` и `CollisionWorld` для высокопроизводительной обработки.
- **No Sync Points**: Структурные изменения (создание/удаление сущностей) выполняются асинхронно через `EntityCommandBuffer`.

### Структура проекта:
```text
Assets/Scripts/
├── Authoring/        # Компоненты-пекари (Baker) для конвертации GameObject в Entity
├── Common/           # Общие утилиты и компоненты (Random, Extensions)
├── Core/             # Теги и базовые определения сущностей
├── Data/             # Blittable IComponentData структуры (только данные)
├── Systems/          # ISystem и SystemBase (вся игровая логика)
└── UI/               # UI Bridge, Camera и прочие классы для связи с non-ECS миром
```

---

## 🛠 Технологический стек

- **Engine:** Unity 6 (6000.0.60f1+)
- **Frameworks:**
  - `Entities` (ECS)
  - `Unity Physics`
  - `Burst Compiler`
  - `Universal Render Pipeline` (URP)

---

## 🎮 Функционал

### Персонаж
- **Перемещение**: WASD управление с физическим движением.
- **Стрельба**: Автоматическая стрельба раз в секунду в ближайшего врага.
- **Состояния**: Блокировка действий при смерти.

### Враги
- **Спавнер**: Периодический спавн врагов за пределами камеры (настраиваемая дистанция).
- **ИИ**: Преследование игрока с остановкой на заданной дистанции атаки.
- **Урон**: Нанесение урона при контакте или на близкой дистанции.

### Экономика и UI
- **Монеты**: Выпадают с врагов (шанс 50%), подбираются при приближении.
- **UI**: Отображение здоровья игрока, количества монет, HealthBar над врагами, экран "Game Over".

---

## ⚙️ Настройка и Конфигурация

Все параметры баланса настраиваются через компонент `GameConfigAuthoring` на объекте `GameConfig` в сцене.

| Параметр | Описание | Значение по умолчанию |
|----------|----------|-----------------------|
| **Enemy Damage Range** | Дистанция нанесения урона врагом | 1.5 |
| **Projectile Hit Radius** | Радиус попадания снаряда | 0.5 |
| **Coin Collection Radius** | Радиус подбора монет | 1.0 |
| **Coin Drop Chance** | Шанс выпадения монеты (0.0 - 1.0) | 0.5 (50%) |
| **Stop Distance** | Дистанция остановки врага перед игроком | 2.0 |
| **Max Enemies** | Максимальное количество врагов на сцене | 100 |
| **Spawn Distance Min** | Минимальная дистанция спавна от игрока | 15.0 |
| **Spawn Distance Max** | Максимальная дистанция спавна от игрока | 20.0 |
| **Default Projectile Speed** | Скорость полета снаряда | 20.0 |

---

## 🚀 Запуск проекта

1. Открыть сцену `Assets/Scenes/Main.unity`.
2. В `Hierarchy` выбрать объект `GameConfig` для настройки параметров (опционально).
3. Нажать **Play** в редакторе Unity.

### Управление:
- **W/A/S/D** — Перемещение персонажа.
- **Авто-стрельба** — Персонаж автоматически стреляет в ближайшего врага.

---

## 📜 Лицензия

Проект создан в рамках выполнения тестового задания для компании Winzardy.
