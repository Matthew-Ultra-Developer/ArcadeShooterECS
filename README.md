# ArcadeShooterECS

Высокопроизводительный аркадный шутер с видом сверху, построенный на **Unity ECS (DOTS)**. Проект демонстрирует применение принципов Data-Oriented Design для создания эффективной и масштабируемой игровой логики.

---

## 🏗 Архитектура

Проект строго следует принципам **чистой ECS архитектуры**. Вся логика отделена от данных, а тяжелые вычисления оптимизированы с помощью Job System и Burst Compiler.

### Основные принципы:
- **Pure ECS**: Игровая логика реализована исключительно в `ISystem` и `SystemBase`. `MonoBehaviour` используется только для Authoring и связи с UI.
- **Job System & Burst**: Все ресурсозатратные системы параллелизированы через `IJobEntity`. Для всех систем и джобов включена Burst-компиляция.
- **Unity Physics**: Обработка коллизий и триггеров через `Unity.Physics`. Используется `ITriggerEventsJob` для высокопроизводительной обработки столкновений без вложенных циклов.
- **No Sync Points**: Структурные изменения (создание/удаление сущностей) выполняются асинхронно через `EntityCommandBuffer`.

### Структура проекта:
```text
Assets/Scripts/
├── Components/       # Blittable IComponentData структуры (только данные)
├── Systems/          # ISystem и SystemBase (вся игровая логика)
├── Authoring/        # Компоненты-пекари (Baker) для конвертации GameObject в Entity
└── Monobehaviours/   # UI Bridge, Camera и прочие вспомогательные классы для связи с non-ECS миром
```

---

## 🛠 Технологический стек

- **Engine:** Unity 6 (6000.0.60f1 / 6000.3.2f1)
- **Frameworks:**
  - `Entities` (ECS)
  - `Unity Physics`
  - `Burst Compiler`
  - `Universal Render Pipeline` (URP)

---

## 📋 Техническое задание (ТЗ)

**Компания:** Winzardy — молодая игровая студия.
**Цель:** Создать прототип аркадной игры с видом сверху.

### Основные требования:
- **Персонаж:** 
    - Здоровье, перемещение WASD, преграды.
    - Автоматическая стрельба раз в секунду в ближайшего врага.
    - Блокировка действий при смерти.
- **Враги:**
    - Спавн раз в 7 секунд за камерой.
    - Движение к игроку, нанесение урона (5 HP/сек) при контакте.
    - Преграды друг для друга и игрока.
- **Экономика:**
    - Монетки (+1 ресурс) с шансом 50% при смерти врага.
- **UI:**
    - HP игрока (LTop), Ресурсы (RTop).
    - HealthBars над врагами (Unity UI).
    - Экран проигрыша с кнопкой рестарта.

### Особенности реализации:
- Визуализация на 3D-примитивах.
- Все параметры (HP, урон, скорости, шансы) настраиваются через инспектор.
- **Обязательно: использование ECS и/или Data Oriented подхода.**

---

## ⚙️ Настройка и запуск

Все параметры баланса настраиваются в Unity Editor. Основные настройки вынесены в префаб игрока и конфиг-сущности:

1. Открыть сцену `Assets/Scenes/Main.unity`.
2. В `Hierarchy` можно найти объекты `Player` и `GameConfig` для настройки параметров.
3. Нажать **Play**.

### Управление:
- **W/A/S/D** — Перемещение.
- **Стрельба** — Автоматическая (по ближайшему врагу).

---

## 📜 Лицензия

Проект создан в рамках выполнения тестового задания.
